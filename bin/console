#!/usr/bin/env ruby
# frozen_string_literal: true

require 'bundler/setup'
require 'fenetre'

puts 'Loading Fenetre console...'
# You can add commands here to be executed when the console starts
# For example:
# require "irb"
# IRB.start(__FILE__)
# Or, if you prefer Pry:
# require "pry"
# Pry.start

# Default to IRB if nothing else is specified
require 'irb'
IRB.start(__FILE__)
